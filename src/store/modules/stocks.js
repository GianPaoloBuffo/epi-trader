// TODO: 2 - Add 'stocks' to the state; and add this module to the store

const state = {

};

const mutations = {
    setStocks(state, stocks) {
        // 2
    },
};

const actions = {
    initStocks({ commit }) {
        // 2
    },
};

const getters = {
    stocks(state) {
        // 2
    },
};

export default {
    state,
    mutations,
    actions,
    getters,
};
